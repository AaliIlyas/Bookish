@model IList<CheckoutViewModel>;

<h1>History of books on loan</h1>

@foreach (var checkout in Model)
{
    <p>@checkout.Book.Title by @checkout.Book.Author, loaned to @checkout.Member.Name, from: @checkout.DateLoaned, until: @checkout.DateDue, returned on date: @checkout.DateReturned</p>
}